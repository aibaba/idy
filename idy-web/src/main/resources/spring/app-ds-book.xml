<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:tx="http://www.springframework.org/schema/tx" xmlns:context="http://www.springframework.org/schema/context" xmlns:util="http://www.springframework.org/schema/util"
	xsi:schemaLocation="
    http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd
    http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd
    http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
    http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd">

	<bean id="dataSource_book" class="org.apache.commons.dbcp.BasicDataSource">
		<property name="driverClassName" value="${book.db.driver}" />
		<property name="url" value="${book.db.url}" />
		<property name="username" value="${book.db.username}" />
		<property name="password" value="${book.db.password}" />
		<property name="connectionProperties" value="${book.db.connectionProperties}" />
		<property name="maxActive" value="${book.db.maxActive}" />
		<property name="initialSize" value="${book.db.initialSize}" />
		<property name="maxIdle" value="${book.db.maxIdle}" />
		<property name="minIdle" value="${book.db.minIdle}" />
		<property name="minEvictableIdleTimeMillis" value="${book.db.minEvictableIdleTimeMillis}" />
		<property name="timeBetweenEvictionRunsMillis" value="${book.db.timeBetweenEvictionRunsMillis}" />
		<property name="validationQuery" value="${book.db.validationQuery}" />
	</bean>
	
	
	<bean id="transactionManager_book" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource_book" />
	</bean>

	<bean id="jdbcTemplate_book" class="org.springframework.jdbc.core.JdbcTemplate">
		<property name="dataSource">
			<ref bean="dataSource_book" />
		</property>
	</bean>

	<bean id="sqlSessionFactory_book" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource_book" />
		<property name="configLocation" value="classpath:/mybatis/mybatis-config-book.xml" />
		<property name="mapperLocations" value="classpath:/mappers/book/*.xml" />
	</bean>

	<bean id="sqlSession_book" class="org.mybatis.spring.SqlSessionTemplate">
		<constructor-arg index="0" ref="sqlSessionFactory_book" />
	</bean>

	<!-- 自动扫描 mapper 接口 -->
	<bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
		<property name="basePackage" value="com.doogua.api.dao.book,com.doogua.api.dao.book.*" />
		<property name="sqlSessionTemplateBeanName" value="sqlSession_book" />
	</bean>

	<!-- 使用annotation定义事务 -->
	<tx:annotation-driven transaction-manager="transactionManager_book" proxy-target-class="true" />

</beans>